fastapi==0.115.0
uvicorn[standard]==0.30.6
httpx==0.27.2
pydantic==2.9.2

# Optional — only needed if you set ANTHROPIC_API_KEY / OPENAI_API_KEY
# to use a real LLM for phrasing chat replies instead of the built-in templates.
# anthropic==0.34.0
# openai==1.51.0
